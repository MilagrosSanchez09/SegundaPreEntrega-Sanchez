<h1>Products Page</h1>

{{#if user}}
    <h2>Welcome, {{user.username}}!</h2>
{{else}}
    <p>Please log in to access personalized content.</p>
{{/if}}

<ul>
    {{#each products}}
        <li>Product Name: {{this.name}} - Description: {{this.description}} - Price: {{this.price}}</li>
    {{/each}}
</ul>
